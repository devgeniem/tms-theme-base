import WebFont from 'webfontloader';

WebFont.load( {
    google: {
        families: [
            // 500, 600 & 800 (bold, italic)
            'Montserrat:wght@0,500;0,600;0,800',
            // 400 (normal, bold, italic), 600 (normal, bold, italic)
            'Open+Sans:ital,wght@0,400;0,600;1,400;1,600',
        ],
    },
} );
