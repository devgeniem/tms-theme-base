<div class="contacts">
    <div class="columns">
        <div class="column is-12">
            {?title}
                <h2>
                    {title|s}
                </h2>
            {/title}

            {?description}
                {description|s}
            {/description}
        </div>
    </div>

    <div class="columns is-mobile is-multiline is-variable is-6">
        {#filled_contacts}
            {>"shared/contact" /}
        {/filled_contacts}
    </div>
</div>
