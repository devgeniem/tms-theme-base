<div class="grid__item gallery gallery--gap0 gallery-columns-1 is-align-items-stretch grid__item--featured is-relative {classes}">
    <div class="grid__item__image is-order-1 image is-square is-clipped">
        {?image.id}
            {@image id=image.id size="large" class="objectfit-image" /}
        {/image.id}
    </div>

    <div class="overlay overlay--dark-30"></div>

    <div class="grid__item__details is-order-0 has-text-left is-absolute has-top-0 has-right-0 has-left-0 is-flex is-align-self-flex-end is-flex-direction-column is-justify-content-end pb-7 pr-6 pl-6 has-text-white">
        {?title}
            <h3 class="h4 mb-0 has-text-white">{title}</h3>
        {/title}

        {?description}
            <p class="has-text-small has-text-white">{description|s}</p>
        {/description}

        {?link}
            <a href="{link.url}" target="{link.target}" class="button has-width-fit-content is-primary ml-0 mt-1">{link.title}</a>
        {/link}
    </div>
</div>
