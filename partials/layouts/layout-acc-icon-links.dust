<section {?anchor}id="{anchor|s}"{/anchor}
         class="acc-icon-links section pt-12 pb-7">
    <div class="container">
        <div class="columns">
            <div class="column">
                <div class="acc-icon-links__grid">
                    {#rows}
                        <div class="acc-icon-links__item keep-vertical-spacing">
                            {?acc_icon}
                                {>"ui/icon" icon=acc_icon class="icon--xxxlarge" /}
                            {/acc_icon}

                            {?title}
                                <h2 class="h4 mt-0 mb-4">
                                    {title|s}
                                </h2>
                            {/title}

                            {?description}
                                <div class="acc-icon-links__item-description mb-5">
                                    {description|s}
                                </div>
                            {/description}

                            {>"ui/link" classes="is-small is-primary" icon=link.icon /}
                        </div>
                    {/rows}
                </div>
            </div>
        </div>
    </div>
</section>
